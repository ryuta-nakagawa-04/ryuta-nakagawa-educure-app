<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.educure.household_account_book_app.repository.mapper.GoalMapper">

  <resultMap id="GoalMap" type="jp.educure.household_account_book_app.entity.Goal">
    <id     property="id"           column="id"/>
    <result property="userId"       column="user_id"/>
    <result property="yearMonth"    column="year_month"/>
    <result property="targetAmount" column="target_amount"/>
    <result property="createdAt"    column="created_at"/>
    <result property="updatedAt"    column="updated_at"/>
  </resultMap>

  <select id="findByUserIdAndYearMonth" resultMap="GoalMap">
    SELECT id, user_id, year_month, target_amount, created_at, updated_at
      FROM goals
     WHERE user_id = #{userId}
       AND year_month = #{yearMonth}
     LIMIT 1
  </select>

  <!-- 更新 -->
  <insert id="upsert" parameterType="jp.educure.household_account_book_app.entity.Goal">
    INSERT INTO goals (user_id, year_month, target_amount)
    VALUES (#{userId}, #{yearMonth}, #{targetAmount})
    ON CONFLICT (user_id, year_month)
    DO UPDATE SET
      target_amount = EXCLUDED.target_amount,
      updated_at    = now()
  </insert>
</mapper>
